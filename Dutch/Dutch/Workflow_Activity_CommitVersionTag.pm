package WebGUI::i18n::Dutch::Workflow_Activity_CommitVersionTag;

our $I18N = {
	'commit version tag body' => {
		message => q|Met deze workflow activiteit wordt een versie tag ingezonden. Dit is de laatste stap in een workflow die met het verzenden van versie tags werkt. Als er bijvoorbeeld een workflow gemaakt is die iemand anders nodig heeft voor het goedkeuren van het inzenden van een versie tag, wordt dit de laatste stap in de workflow. De uitvoering hangt af van de daadwerkelijke goedkeuring.|,
		lastUpdated => 1166638045
	},

	'activityName' => {
		message => q|Inzenden versie tag|,
		lastUpdated => 1155545036
	},

};

1;
